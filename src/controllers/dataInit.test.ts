import { Request, Response } from 'express';

import { dataInitController } from './dataInit';

describe('dataInit controller', () => {
  describe('viewInitialData method', () => {
    it('needs finished tests', () => {
      expect(1).toEqual(1);
    });
  });
});